---
description: Fix TypeScript, ESLint, and Tailwind CSS bugs from JSON error format
argument-hint: [paste-json-errors-here]
allowed-tools: Read, Edit, Write, Bash
---

# Bug Fixer

I'm ready to fix bugs from JSON error format. Please paste the error array JSON and I'll process each error systematically.

## Error Types I Handle:

1. **TypeScript Errors** (severity 8):
   - Type mismatches
   - Ref type issues
   - Interface compatibility
   - Generic type problems

2. **Tailwind CSS Suggestions** (severity 4):
   - Replace `bg-gradient-to-*` with `bg-linear-to-*`
   - Replace `flex-shrink-0` with `shrink-0`
   - Replace `grayscale-[30%]` with `grayscale-30`
   - Other canonical class improvements

3. **ESLint Violations**:
   - Code style issues
   - Best practice violations

## My Fix Process:

For each error:

1. Read the affected file at the specified line
2. Apply the appropriate fix based on error type
3. Preserve functionality while improving code quality
4. Move to next error

## Ready!

Paste your JSON error array below and I'll start fixing them one by one.
